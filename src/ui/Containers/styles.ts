import styled from '@emotion/styled'

const flexCenter = `
  display: flex;
  justify-content: center;
  align-items: center;
`

export const ContainerHeader = styled.div`
  width: 1600px;
  background: #fff;
  ${flexCenter}
`
